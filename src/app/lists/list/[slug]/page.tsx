// This will probably not be used. 
// As the editing of list should be done in a component and from the main /lists page

export default async function Page() {
    return <h1>Page for editing a single list.</h1>
}