// I would like to eventually make this possible from the /lists route
// For ease of development I will probably make this a separate route at first

export default async function Page() {
    return <h1>Page for adding a single list.</h1>
}